query playlistQuery($playlistId: ID!) {
  playlist(playlistId: $playlistId) {
    id
    name
    songs {
      id
      title
      artist
      createdAt
    }
    user {
      id
      name
    }
  }
}
